<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.serviceprovider.mapper.OrderMapper">

    <insert id="saveOrder">
        insert into orders (order_id, order_name) values(#{orderId}, #{orderName});
    </insert>

</mapper>